import { SelectedGridBoxDirective } from './selected-grid-box.directive';

describe('SelectedGridBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedGridBoxDirective();
    expect(directive).toBeTruthy();
  });
});
